// function ElementFactory(name, type, className, id, attributes) {
//   var element = {};

//   element.name = name;
//   (element.type = document.createElement(type)),
//     (element.className = className);
//   element.id = id;
//   return element;
// }
// element crées non reconnus comme node: probleme avec appendChild